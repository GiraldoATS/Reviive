# Modelo de Machine Learning — pendiente

El enunciado académico exige un modelo simple (clasificación, regresión o
clustering) entrenado en Colab/Jupyter/Anaconda, con dataset, modelo,
resultado y gráfica documentados, basado en el temario hasta la Unidad 7.

Esta carpeta es el lugar donde debe vivir ese notebook (`.ipynb`) una vez
se decida el caso de uso (por ejemplo: clasificar el estado de un objeto a
partir de su descripción para sugerir el servicio de restauración, o
estimar el tiempo de restauración a partir de las características del
objeto). Aún no se ha construido — no estaba en el alcance de esta
entrega (frontend + arquitectura de backend).
